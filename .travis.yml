language: node_js

services:
  - elasticsearch

before_script:
  - sleep 10

node_js:
  - "node"

script:
  - npm run test-ci

deploy:
  provider: script
  script: ./deploy.sh
  on:
    branch: master
